# 项目交付规则

## 完成标准

- 功能实现后，必须运行与改动匹配的本地验证，并确认工作区没有误纳入的无关修改。
- 验证通过后，提交本次任务改动并推送到当前项目远端；交付说明必须包含提交号、推送分支和验证结果。
- 不能以“代码已完成”替代提交、推送和部署结果；任何阻断都必须明确说明。

## 正式部署

- 本项目正式服务是 `server/app.py` 的 Python 服务，部署目标为腾讯云 `ubuntu@124.222.83.113` 的 `/opt/game-team-board`；不要把 Vinext、D1、本地 `.wrangler` 或 `.next` 原型当作正式服务发布。
- 正式服务为 `game-team-board.service`，数据库位于 `/var/lib/game-team-board/tasks.db`。替换正式文件前，先在服务器保存带时间戳的备份；不得删除或覆盖数据库及用户数据。
- 只发布本次任务需要的正式服务文件，不上传密钥、环境变量、个人数据、依赖缓存或构建临时目录。静态资源改动要校验线上 HTTP 返回内容与本地版本一致。
- 涉及服务端代码或运行时配置时，只重启 `game-team-board.service`；修改 Nginx 前必须先执行配置测试，测试失败不得 reload。发布后检查服务状态、正式入口和本次功能的关键交互。
- 部署完成后，再核对本地 Git 状态、远端提交和服务器实际版本，并在交付说明中报告备份位置、服务状态及未解决的告警。
